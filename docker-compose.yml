services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: agentic_pay
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - agentic_pay_pg:/var/lib/postgresql/data
volumes:
  agentic_pay_pg:
