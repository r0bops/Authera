# Authera

Frontend React para crear mandatos de compra asistidos. Incluye un flujo de viaje de tres pasos, reglas de presupuesto y una vista previa que se actualiza en tiempo real.

## Desarrollo

```bash
npm install
npm run dev
```

Para generar una compilación de producción:

```bash
npm run build
```
