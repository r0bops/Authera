/**
 * Drizzle schema. Phase 2 adds the tables from CLAUDE_IMPLEMENTATION_SPEC.md §10
 * (users, agents, agent_keys, mandates, mandate_versions, mandate_runtime, offers,
 * checkouts, nonces, idempotency_records, executions, reservations, approval_requests,
 * payments, webhook_events, disputes, audit_chain_heads, audit_events).
 */
export {};
